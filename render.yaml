services:
  - type: web
    name: mailgenie-backend
    runtime: docker
    rootDir: email-writer-sb
    dockerfilePath: ./email-writer-sb/Dockerfile
    envVars:
      - key: PORT
        value: 8080
      - key: SPRING_PROFILES_ACTIVE
        value: production
